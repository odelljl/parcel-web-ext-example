{
  "targets": {
    "webext-dev": {
      "sourceMap": {
        "inline": true,
        "inlineSources": true
      }
    },
    "webext-prod": {
      "sourceMap": false
    }
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "cd src && parcel build manifest.json --target webext-prod --dist-dir ../dist"
  },
  "dependencies": {
    "jquery": "^3.6.0",
    "bootstrap": "^5.1.3"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.0.0",
    "@parcel/transformer-image": "2.0.0-rc.0",
    "@parcel/transformer-typescript-tsc": "^2.0.0",
    "@types/bootstrap": "^5.1.6",
    "@types/chrome": "^0.0.158",
    "@types/jquery": "^3.5.6",
    "parcel": "^2.0.0-rc.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.2"
  }
}
